# Frontend

React + TypeScript + Vite UI for the Football Intelligence Lab API.

```bash
make api        # backend on :8000
make frontend   # Vite on :5173
```

The dev server proxies `/api` and `/health` to `http://127.0.0.1:8000`.
